# Cấu hình cơ bản ứng dụng
spring.application.name=base_be_java
server.address=0.0.0.0
server.port=8888

# Cấu hình database đơn giản
spring.datasource.url=jdbc:postgresql://localhost:5432/novels_app
spring.datasource.username=postgres
spring.datasource.password=12345
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Bỏ qua trường null khi trả về JSON
spring.jackson.default-property-inclusion=NON_NULL

# JWT đơn giản (vẫn cần nhưng dùng key đơn giản)
jwt.signerKey="Vwlhn8GsLwCylorej1Wdtijgzrdr6RWy"

# Tắt SSL (chỉ dùng HTTP)
server.ssl.enabled=false
